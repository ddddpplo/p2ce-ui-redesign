'use strict';

class ChapterSelect {
	static panels = {
		cp: $.GetContextPanel(),
		chapterTitle: $('#ChapterTitle'),
		chapterThumbnail: $('#ChapterThumbnail')
	};
	
	static clickChapterButton(chapterNum: int8) {
		
	}
}
